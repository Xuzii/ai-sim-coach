target-version = "py310"
line-length = 120
src = ["src", "tests"]

[lint]
# pycodestyle errors/warnings, pyflakes, import sorting, pyupgrade, bugbear.
select = ["E", "W", "F", "I", "UP", "B"]

[lint.isort]
known-first-party = ["pitwall"]
